package me.fexus.audio.openal



fun main() {
    /*
val audioData: DefaultAudioFileData = OpenALAudioSystem.loadAudioDataFromResource("sound/mine.ogg")

 OpenALAudioSystem.destroy()

 val soundSource = AudioSource()
     .setGain(1f)
     .setPosition(Vec3(0))
     .setVelocity(Vec3(0))
     .setLooping(false)

 val soundBuffer = AudioBuffer()
 alBufferData(soundBuffer.bufferID, audioData.openALFormat, audioData.data, audioData.header.sampleRate)

 soundSource.setBuffer(soundBuffer)

 soundSource.play()

 while (soundSource.isPlaying()) {  }

 AudioSystem.destroy()
  */
}